#include <stdio.h>
#include "Graph.h"

int main() {
  Graph g(5);
  g.AddEdge(1, 2, 3);
 // printf("%i\n", g.IsConnect(1, 2));
//  printf("%i\n", g.IsConnect(2, 1));
//  printf("%i\n", g.IsConnect(3, 1));
//  printf("%i\n", g.IsConnect(1, 3));
//  printf("%i\n", g.IsConnect(4, 5));
}
